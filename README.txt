this requires the
	pygame
	pyaudio
	speech_recognition
libraries...

not sure if pygame can be downloaded with pip
but the other two:
python -m pip install pyaudio
python -m pip install SpeechRecognition